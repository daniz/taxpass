- name      = @name ? @id
- className = @className ? "request-data"
- value     = @value ? @model[name]

%label{ for: @id }= @label
.date-field
  %input.request-data.date-input{ id: @id, name: name, "class": className, value: value }