- checked   = @checked ? @model?[ @id ]
- name      = @name ? @id
- className = @className ? "request-data"

%input{ "class": className, id: @id, name: name, type: "checkbox", value: "1", checked: !!checked }

- if @label
  %label{ for: @id }= @label
